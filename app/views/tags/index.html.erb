<h1>All Tags</h1>

<ul>
<% @tags.each do |tag| %>
  <li><%= link_to tag.name, tag_path(tag)%><span style="margin-left: 10px;"><%= link_to 'Delete', tag_path(tag), method: :delete, data: {confirm: "Are you sure?"} %></span></li>
<% end %>
</ul>
